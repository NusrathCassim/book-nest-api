$kind: http-request
name: Update Booking Status
url: "{{baseUrl}}/bookings/{{bookingId}}/status"
method: PATCH
body:
  type: json
  content: "{\r\n  \"status\": \"CONFIRMED\"\r\n}"
auth:
  type: bearer
  credentials:
    token: "{{accessToken}}"
order: 6000
